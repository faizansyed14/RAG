# Tree-indexing/tree-search engine -- vendored directly into app/rag_core/
# (copied from a local reference build, renamed to this project's naming),
# not pip-installed. These are its own runtime dependencies, carried over
# from its real requirements so the vendored copy still runs correctly.
# openai-agents is NOT optional, despite living next to the cloud/MCP
# integrations we don't use: own-model chat's tool-calling loop
# (rag_core/local_chat.py) is built on top of it directly -- confirmed by
# it actually failing at runtime without this line, not assumed.
Pillow>=9.0
PyPDF2>=3.0.0
litellm>=1.97.0
openai-agents>=0.18.1
pypdfium2>=5
pyyaml>=6.0
regex>=2024.0.0
requests>=2.28.0
sortedcontainers>=2.4.0
urllib3>=1.26

# Backend framework
fastapi>=0.115
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pydantic>=2.7
pydantic-settings>=2.3
python-dotenv>=1.0

# Postgres
sqlalchemy>=2.0
asyncpg>=0.29
alembic>=1.13
psycopg2-binary>=2.9      # sync driver -- rag_core/postgres_store.py runs off the event loop

# Vector DB for diagram embeddings
qdrant-client>=1.11

# Object storage (original files + page/diagram crops)
boto3>=1.34

# Model gateway (OpenRouter, OpenAI-compatible)
openai>=1.40

# Document parsing
pypdf>=4.2
python-docx>=1.1
openpyxl>=3.1        # XLSX ingestion
pymupdf>=1.24       # page rasterization for scanned/diagram pages
reportlab>=4.2       # renders DOCX/CSV/XLSX/EML/OCR content to PDF for the tree engine

# Auth
pyjwt>=2.9
argon2-cffi>=23.1       # password hashing for the users table
